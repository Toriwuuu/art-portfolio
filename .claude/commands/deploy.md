# Deploy

執行以下步驟將專案部署到 Cloudflare Pages：
1. 執行 `npm run build`
2. 執行 `git add -A`
3. 執行 `git commit -m "deploy: update site"`
4. 執行 `git push`
5. 告知使用者去 GitHub Actions 查看部署進度，網址是 https://art-portfolio-8cl.pages.dev
